{
  "private": true,
  "scripts": {
    "clean": "rimraf css/*",
    "watch": "npm run clean && sass --watch scss:css",
    "dev": "npm run clean && sass scss:css",
    "build": "npm run clean && sass --style compressed scss:css"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "sass": "^1.55.0"
  }
}
